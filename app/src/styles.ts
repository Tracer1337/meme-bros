import { StyleSheet } from "react-native"

const globalStyles = StyleSheet.create({
    handle: {
        backgroundColor: "#fff",
        borderColor: "#000",
        borderWidth: 1,
        borderRadius: 4
    }
})

export default globalStyles
